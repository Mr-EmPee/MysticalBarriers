# Prevent chorus fruit teleportation within 8 blocks of a barrier
block-chorus-teleportation: true

# Prevent entities from getting through barriers
block-entities:
  enabled: true
  exclude:
    - ARMOR_STAND

repel-projectiles:
  enabled: true
  # Enable only for projectiles shot by a player
  only-from-player: false

version: 2
